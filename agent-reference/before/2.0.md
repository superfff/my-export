作为前端工程师，第一次创建全栈项目，目前前端框架已确定。

## 目标
这一次想要实现简单的端对端，实现订单管理的页面调用后端的请求查询。

## 后端代码
我本地的环境已经安装了 jdk21，安装了 docker环境、安装了 mySQL 8.4。
1. 后端使用 java，使用 spring boot 框架，利用 MVC分层的模式维护清晰的 controller、service、mapper，配合前端请求维护清晰的DTO，代码逻辑不要揉在一起。使用 Maven 管理后端中的依赖
2. 使用 mySQL 作为数据库管理，我提前预设了订单管理要展示的信息，帮我对应的创建一张数据库表和字段信息，并看一下如果有我没考虑到字段，帮我补全
   - 订单号、客户名称、客户手机号、状态状态、订单创建时间
   - 订单状态暂定：未支付、已支付、已取消
3. 接口使用 /api 作为统一的前缀，第一次的端对端版本，先只提供一个查询的接口请求URL为 /order
4. 维护一个 /actuator/health 检测后端服务是否正常
5. 在后端代码中也是清晰分好文件夹目录，并且通过命名来区分模块和模块中对应的功能

## 前端代码
1. 在原有的代码基础上修改为 typescript 的格式
2. 使用 fetch 作为请求的工具，创建 ./http/ 文件夹用于定义 fetch 的请求，以低耦合作为标准，维护底层的 fetch，以及包装后的请求方法
3. 实现订单管理的查询接口，使用 get 请求进行接口发送，并且带上页面的筛选条件
4. 删掉原有 mock 数据相关的代码和文件

## 工程
帮我使用 docker 来管理前端和后端，代码结构你帮我调整一下，实现别人拿到我的工程，直接用docker就能部署。

## 输出和验收标准
因为我是前端工程师，第一次接触全栈，关于后端和docker的改造实现，改造完成后，请给我一步步的指引，用简单直白的方式告诉我是怎么实现的，让我一看就能明白。

执行完成后，我在前端界面中能看到请求的发起并没有报错算是验收通过