作为前端工程师，第一次创建全栈项目，目前前后端的框架已确定。

本阶段的目标：
- 订单管理的查询条件优化：区分草稿模式的查询条件、确定传递到后端的查询条件
- 表格组件增加数据勾选功能，跨页时数据保留不丢失，当有勾选数据时，在表格上方动态展示已勾选多少条的提醒，没勾选数据时隐藏
- 在表头加入支持2种模式的选择场景：手动勾选数据、全选数据
    - 手动勾选数据模式，保存数据的id信息
    - 全选数据模式，有标识表示全选，保留筛选条件、全部数据id不记录，只记录手动反选数据的id（如果有的时候）

- 前端加入定时器，结合 后端健康请求，每2秒查询一次，如果出现后端异常，固定在管理系统的右上角提示“后端服务异常”的文案。

验收标准：
1. 请逐一实现本阶段的目标，并告知增加或修复了什么内容
2. 每完成一个步骤都进行测试